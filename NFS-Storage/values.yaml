deployment:
  namespace: 'default'

nfs:
  pv:
    accessModes:
      - ReadWriteMany
    size: 2Gi
    mountPath: '/pv/nfs'
    storageName: 'nfs-storage'
    storageClassName: 'nfs-storage-class'
    claimName: 'nfs-pvc'